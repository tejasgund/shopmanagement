# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Runtime output
logs/
uploads/          # meter photos - evidence files, backed up separately, never in git

# Local env
.env
*.db


# Scheduler runtime output (each script logs into its own folder)
scheduler/*/logs/*.log
scheduler/*/logs/*.gz
